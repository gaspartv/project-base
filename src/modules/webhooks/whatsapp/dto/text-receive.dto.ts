/**
 * Negrito = Asterisco ( * ) \
 * Itálico = Sublinhado ( _ ) \
 * Riscado = Til ( ~ ) \
 * Code = Três acentos graves ( ``` )
 */
export interface TextReceiveDto {
  body: string // Maximum length: 4096
}
